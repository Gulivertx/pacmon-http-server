{
  "name": "pacmonitor-simple-server",
  "version": "1.0.0",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon",
    "build": "tsc && tsc --project tsconfig.json",
    "start": "NODE_ENV=production node dist/index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.1",
    "luxtronik2": "^2.6.0"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "4",
    "@types/node": "16",
    "nodemon": "^2.0.16",
    "typescript": "^4.6.4"
  }
}
